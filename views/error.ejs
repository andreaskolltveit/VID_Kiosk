<!-- Display the main message using an h1 element, dynamically populated by the 'message' variable. -->
<h1><%= message %></h1>

<!-- Display the error status using an h2 element, dynamically populated by the 'error.status' variable. -->
<h2><%= error.status %></h2>

<!-- Display the error stack trace using a pre element, dynamically populated by the 'error.stack' variable. -->
<pre><%= error.stack %></pre>